# Rhythm & Loot - 部署指南

## 🚀 快速部署

本应用支持两种主流部署平台：**Vercel** 和 **Netlify**。两者都提供免费套餐，非常适合个人项目。

---

## 选项 1: Vercel 部署（推荐）

### 为什么选择 Vercel？
- ✅ 与 Vite 完美集成
- ✅ 自动 HTTPS
- ✅ 全球 CDN
- ✅ 零配置部署

### 部署步骤

#### 方法 A: 通过 GitHub（推荐）

1. **推送代码到 GitHub**
   ```bash
   git add .
   git commit -m "Ready for deployment"
   git push origin main
   ```

2. **连接 Vercel**
   - 访问 [vercel.com](https://vercel.com)
   - 使用 GitHub 账号登录
   - 点击 "New Project"
   - 导入你的 GitHub 仓库
   - Vercel 会自动检测 Vite 配置

3. **配置项目**
   - Framework Preset: `Vite`
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Install Command: `npm install`

4. **部署**
   - 点击 "Deploy"
   - 等待 2-3 分钟
   - 完成！你会获得一个 `.vercel.app` 域名

#### 方法 B: 通过 Vercel CLI

```bash
# 安装 Vercel CLI
npm install -g vercel

# 登录
vercel login

# 部署
vercel

# 部署到生产环境
vercel --prod
```

---

## 选项 2: Netlify 部署

### 为什么选择 Netlify？
- ✅ 简单易用
- ✅ 自动 HTTPS
- ✅ 表单处理（如需要）
- ✅ 免费套餐慷慨

### 部署步骤

#### 方法 A: 通过 GitHub

1. **推送代码到 GitHub**（同上）

2. **连接 Netlify**
   - 访问 [netlify.com](https://netlify.com)
   - 使用 GitHub 账号登录
   - 点击 "Add new site" → "Import an existing project"
   - 选择你的 GitHub 仓库

3. **配置构建设置**
   - Build command: `npm run build`
   - Publish directory: `dist`
   - 点击 "Deploy site"

4. **完成**
   - 等待构建完成
   - 你会获得一个 `.netlify.app` 域名

#### 方法 B: 拖放部署

```bash
# 构建项目
npm run build

# 将 dist 文件夹拖放到 Netlify 网站
# 访问 app.netlify.com/drop
```

---

## 📱 安装为 PWA

部署完成后，用户可以将应用安装到手机：

### Android 设备

1. 使用 Chrome 浏览器访问部署的网址
2. 点击浏览器菜单（三个点）
3. 选择 "添加到主屏幕" 或 "安装应用"
4. 确认安装
5. 应用图标会出现在主屏幕上

### iOS 设备

1. 使用 Safari 浏览器访问部署的网址
2. 点击分享按钮（方框带箭头）
3. 向下滚动，选择 "添加到主屏幕"
4. 点击 "添加"
5. 应用图标会出现在主屏幕上

---

## 🔧 环境变量（可选）

如果将来需要添加 API 密钥或其他配置：

### Vercel
在项目设置 → Environment Variables 中添加

### Netlify
在 Site settings → Build & deploy → Environment 中添加

---

## 📊 监控和分析

### Vercel Analytics
- 在项目设置中启用 Analytics
- 查看访问量、性能指标

### Netlify Analytics
- 升级到付费套餐后可用
- 或使用免费的 Google Analytics

---

## 🐛 常见问题

### 问题 1: Service Worker 不工作
**解决方案**: 确保使用 HTTPS（Vercel/Netlify 自动提供）

### 问题 2: 音频权限被拒绝
**解决方案**: 
- 确保使用 HTTPS
- 在浏览器设置中允许麦克风权限

### 问题 3: 图片加载失败
**解决方案**: 
- 检查 `public/` 文件夹中的图片路径
- 确保所有图片都已提交到 Git

### 问题 4: 构建失败
**解决方案**:
```bash
# 本地测试构建
npm run build

# 检查是否有错误
npm run preview
```

---

## 🔄 更新部署

### 自动部署（推荐）
推送到 GitHub 后，Vercel/Netlify 会自动重新部署：

```bash
git add .
git commit -m "Update features"
git push origin main
```

### 手动部署
```bash
# Vercel
vercel --prod

# Netlify
netlify deploy --prod
```

---

## 📝 部署检查清单

部署前确保：

- [ ] 所有代码已提交到 Git
- [ ] `npm run build` 本地构建成功
- [ ] `npm run preview` 预览正常
- [ ] 所有图片资源在 `public/` 文件夹中
- [ ] `manifest.json` 配置正确
- [ ] Service Worker 路径正确

---

## 🎉 部署成功后

1. **测试功能**
   - 在手机上访问网址
   - 测试音频检测
   - 测试开箱动画
   - 测试离线功能

2. **分享给家人**
   - 复制部署的网址
   - 发送给孩子们
   - 指导他们安装到主屏幕

3. **收集反馈**
   - 观察使用情况
   - 记录问题和改进建议
   - 持续优化

---

## 📞 需要帮助？

- Vercel 文档: https://vercel.com/docs
- Netlify 文档: https://docs.netlify.com
- Vite 文档: https://vitejs.dev

祝部署顺利！🚀
